apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fortigate-firmwares
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-csi-truenas-local
  volumeMode: Filesystem